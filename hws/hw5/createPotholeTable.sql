CREATE TABLE potholes(
creation_date DATE,
service_request_number CHAR(11),
most_recent_action VARCHAR(100),
potholes_filled REAL,
street_address VARCHAR(60),
zip INTEGER,
latitude DOUBLE PRECISION,
longitude DOUBLE PRECISION
);
